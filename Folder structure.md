explainit/
│
├── bin/
│   └── explainit.js          ← CLI entry point (commands live here)
│
├── lib/
│   ├── ai.js                 ← Gemini API calls (generate questions, evaluate answers)
│   ├── quiz.js               ← Quiz logic (loop through questions, show results)
│   └── scores.js             ← Save & read scores from local JSON file
│
├── node_modules/             ← installed packages (never touch this)
│
├── .env                      ← your Gemini API key (never commit this)
├── .gitignore                ← ignores node_modules and .env
├── Folder structure.md       ← your notes (good habit!)
├── LICENSE
├── package-lock.json         ← auto-generated by npm (never touch)
└── package.json              ← project config + dependencies



- Things to remember (explainit.js)

#!/usr/bin/env node = make your JS file executable as a CLI using Node.js

commander -> builds CLI commands (scan, quiz, etc.)
chalk -> colors your terminal output🎨
fs -> reads files from system
path -> handles file paths safely

Create CLI program -> const program = new Command() -> program = your CLI app